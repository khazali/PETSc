${MPIEXEC} ${MPIARGSIZE} ${EXEC} ${ARGS} > ${TESTNAME}.tmp 2>&1
${DIFF} ${SRCDIR}/output/${OUTPUTFILE} ${TESTNAME}.tmp || printf "${PWD}\nPossible problem with with ${TESTNAME}
${RM} -f ${TESTNAME}.tmp
